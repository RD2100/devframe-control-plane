# H2 Integration Example

Using devframe-control-plane to drive dev-frame-opencode task pipeline.

## Dry-run

```bash
devframe run --pipeline pipelines/devframe_opencode.yaml --dry-run
```

Expected: 3 stages, GPT review on authorization and execution_closure.

## Status

Design only. No live execution in H2.
