# DevFrame Control Plane — Environment Variables (EXAMPLE ONLY)
# Copy to .env and fill in real values. .env is gitignored.

# GPT Conversation (placeholder — no real value)
GPT_CONVERSATION_ID=PLACEHOLDER_CONVERSATION_ID

# CDP Configuration (placeholder)
CDP_HOST=localhost
CDP_PORT=9222

# Project paths (placeholder)
CONTROL_PLANE_ROOT=.
WORKSPACE_DIR=workspace/
EVIDENCE_DIR=evidence_packs/
