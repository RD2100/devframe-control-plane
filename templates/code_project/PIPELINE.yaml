# {{PROJECT_NAME}} — Pipeline Definition
# Generated by DevFrame Control Plane (code_project template)

pipeline_id: {{PIPELINE_ID}}
version: 1

global_invariants:
  guard_removal_approved: false
  evidence_cleanup_approved: false

stages:
  - id: g0_bootstrap
    type: bootstrap
    description: "项目冷启动骨架"
    requires_gpt_acceptance: false

  - id: g1_pipeline_definition
    type: planning
    description: "定义流水线 Schema 和状态转换规格"
    depends_on: g0_bootstrap
    requires_gpt_acceptance: false

  # 后续阶段由用户根据项目需求扩展
  # - id: authorization_phase
  #   type: authorization
  #   depends_on: g1_pipeline_definition
  #   requires_gpt_acceptance: true
